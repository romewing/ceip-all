spring:
  application:
    name: spider
  batch:
    initializer:
      enabled: true
  datasource:
    username: root
    type: com.alibaba.druid.pool.DruidDataSource
    password: root
    url: jdbc:MySQL://localhost:3306/ceip_spider
  jpa:
    show-sql: true
